// Weights
// Direction craft is facing is 50% more likely to come up
// When enemy is in line-of-sight or radar, 100% chance to move toward enemy

class EnemyEngine {
  constructor() {
    console.log('new');
  }

  top = 0;

  left = 0;

  // Move toward enemy
  // Move away from enemy
  getNextMove(monoCraftPosition) {
    // if (this.isInLightOfSight(monoCraftPosition))
  }

  // Is in line of sight
  // is in radar sight

  // Fire at enemy
}

export default EnemyEngine;
